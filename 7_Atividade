programa
{
	
	funcao inicio()
	{
		// Declaracao Notas
		real nota1, nota2, nota3, nota4

		// Input
		escreva("De a Primeira Nota: ")
		leia(nota1)

		escreva("De a Segunda Nota: ")
		leia(nota2)

		escreva("De a Terceira Nota: ")
		leia(nota3)

		escreva("De a Quarta Nota: ")
		leia(nota4)

		// Output
		real Soma = nota1 + nota2 + nota3 + nota4
		escreva("Tua media é ", Soma / 4)
	}
}
